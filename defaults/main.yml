## Homebrew

# homebrew_taps:
#   - caskroom/cask

# You may specify 'install_options' and 'state' that will be passed
# to the homebrew module. If you specify either of these options,
# you must specify the 'name' field as well:
# - name: rbenv
#   state: HEAD
#   options: 'some options here'
#
homebrew_packages:
  - htop
  - coreutils
  - ssh-copy-id
  - tmux
  - watch
  - wget
  - git


homebrew_cask_apps:
  - google-chrome
